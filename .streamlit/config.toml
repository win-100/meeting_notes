[server]
# The browser video converter sends the resulting MP3 as a component value.
# The base64 payload is larger than the MP3 itself, so long recordings need a
# larger WebSocket message limit than Streamlit's default.
maxMessageSize = 500
